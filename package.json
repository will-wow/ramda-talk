{
  "name": "ex-payroll-deck",
  "version": "0.0.0",
  "dependencies": {
    "react": "^16.3.2",
    "react-dom": "^16.3.2",
    "spectacle-image-slide": "^1.0.1",
    "spectacle-scripts": "2.0.0"
  },
  "scripts": {
    "start": "yarn react-app-rewired start",
    "build": "yarn react-app-rewired build",
    "deploy": "yarn build && surge -p ./build --domain ramda-talk.surge.sh",
    "test": "yarn react-app-rewired test --env=jsdom",
    "format": "yarn prettier --write **/*.{js,json}"
  },
  "devDependencies": {
    "eslint": "^5.0.0",
    "eslint-config-airbnb": "^17.0.0",
    "eslint-config-prettier": "^2.9.0",
    "eslint-plugin-import": "^2.13.0",
    "eslint-plugin-jsx-a11y": "^6.0.3",
    "eslint-plugin-react": "^7.10.0",
    "prettier": "^1.12.1",
    "raw-loader": "^0.5.1",
    "react-app-rewire-eslint": "^0.2.3",
    "react-app-rewired": "^1.4.0",
    "react-scripts": "^1.1.4"
  }
}
